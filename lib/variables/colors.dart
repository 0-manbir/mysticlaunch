import 'package:flutter/material.dart';

// dark mode
const Color backgroundColor = Colors.black;
final Color lightBackgroundColor = Colors.grey[800]!;
const Color opaqueBackgroundColor = Color.fromARGB(60, 255, 255, 255);

final Color? lightTextColor = Colors.grey[300];
final Color? darkTextColor = Colors.grey[500];


// light mode (also remove the dimness of the icons from right_screen.dart and left_screen.dart)
// const Color backgroundColor = Colors.white;
// final Color lightBackgroundColor = Colors.grey[100]!;
// const Color opaqueBackgroundColor = Color.fromARGB(50, 200, 200, 200);

// final Color? lightTextColor = Colors.grey[400];
// final Color? darkTextColor = Colors.grey[500];
